[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "study-agent"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["ingestion*", "vectorstore*", "retrieval*", "chat*", "app*"]